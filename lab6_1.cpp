#include<iostream>
using namespace std;

int main(){

    int i = 0;
    int num_source[];
    int N = 0;
 
    while(i < 1) {
        cout << "Enter an integer: ";
        cin >> num;
        num = num_source[N]
        N++;

        if(num == 0) {
            i++;
        }
    }

    cout << "#Even numbers = ";
    cout << "#Odd numbers = ";
    return 0;
}
